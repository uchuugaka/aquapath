{
    IBClasses = (
        {
            ACTIONS = {evaluate = id; myAction = id; selectPath = id; }; 
            CLASS = FirstResponder; 
            LANGUAGE = ObjC; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}